# User Interaction Rules

- **Interactive Selection UI**: Whenever asking the user to choose among options, select a direction, or clarify preferences, always use the `ask_question` tool so that choices are rendered in an interactive selection UI modal with multiple choice options.
